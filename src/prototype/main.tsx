// PROTOTYPE entry — notch-native progress treatments (MO-23)
// Delete this file when the prototype question is answered.
import React from 'react'
import ReactDOM from 'react-dom/client'
import { NotchProgressPrototype } from './NotchProgressPrototype'

ReactDOM.createRoot(document.getElementById('root')!).render(
  <React.StrictMode>
    <NotchProgressPrototype />
  </React.StrictMode>,
)
